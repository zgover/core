# @aglyn/aglyn

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build aglyn` to build the library.

## Running unit tests

Run `nx test aglyn` to execute the unit tests via [Jest](https://jestjs.io).
